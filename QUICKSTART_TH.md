# 🚀 คู่มือเริ่มต้นใช้งาน (Quick Start Guide)

## สิ่งที่เพิ่มเข้ามา

ระบบ Minio Simple Bucket Management ของคุณได้รับการอัปเกรดเพิ่มระบบ **Login และ Role-based Access Control** แล้ว! 🎉

## 🔑 สิ่งสำคัญที่ต้องรู้

### บัญชีผู้ใช้เริ่มต้น

มี 2 บัญชีที่สร้างไว้ให้แล้ว:

#### 👑 บัญชี Admin
- **Username:** `admin`
- **Password:** `admin123`
- **สิทธิ์:** ทำทุกอย่างได้ - เห็นทุก bucket (ทั้ง public และ private), สร้าง/ลบ bucket, แก้ไข policy

#### 👤 บัญชี User
- **Username:** `user`
- **Password:** `user123`
- **สิทธิ์:** เห็นและใช้งานได้เฉพาะ bucket ที่เป็น public

## 📋 ขั้นตอนการติดตั้งและรัน

### 1. ติดตั้ง Dependencies
```bash
pnpm install
```

### 2. ตั้งค่า Environment Variables
สร้างไฟล์ `.env` ในโฟลเดอร์หลักของโปรเจค:

```env
# MinIO Configuration
MINIO_ENDPOINT=127.0.0.1
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_PREVIEW_BASE=http://127.0.0.1:9000

# Authentication Configuration
DATABASE_URL="file:./dev.db"
JWT_SECRET="your-secret-key-change-in-production-please"
```

### 3. สร้าง Database
```bash
npx prisma migrate dev
```

คำสั่งนี้จะ:
- สร้างไฟล์ database SQLite (`prisma/dev.db`)
- สร้างตาราง User

### 4. เพิ่มข้อมูลผู้ใช้เริ่มต้น
```bash
pnpm db:seed
```

คำสั่งนี้จะสร้างบัญชี admin และ user ให้

### 5. รันเซิร์ฟเวอร์
```bash
pnpm dev
```

### 6. เปิดเว็บ
เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`

คุณจะถูก redirect ไปหน้า login อัตโนมัติ

## 🎮 การใช้งาน

### ทดลองใช้งานเป็น Admin

1. **Login:**
   - เปิด `http://localhost:3000`
   - กรอก username: `admin`
   - กรอก password: `admin123`
   - กด Sign in

2. **สิ่งที่คุณจะเห็น:**
   - เห็นชื่อผู้ใช้และ badge "ADMIN" สีเหลืองที่มุมบนซ้าย
   - เห็น bucket ทั้งหมด (ทั้ง public 🌐 และ private 🔒)
   - มีปุ่ม "Create Bucket" ให้กดสร้าง bucket ใหม่
   - มีปุ่ม Edit (รูปดินสอ) ที่แต่ละ bucket

3. **ทดสอบสิทธิ์:**
   - สร้าง bucket ใหม่
   - อัปโหลดไฟล์
   - ลบไฟล์/folder
   - แก้ไข bucket policy (Private/Public Read)
   - ลบ bucket

### ทดลองใช้งานเป็น User

1. **Logout จาก Admin:**
   - กดปุ่ม "Sign Out" ด้านล่างซ้าย

2. **Login ใหม่:**
   - Username: `user`
   - Password: `user123`

3. **สิ่งที่คุณจะเห็น:**
   - เห็นชื่อผู้ใช้และ badge "USER" สีน้ำเงิน
   - เห็นเฉพาะ bucket ที่เป็น public 🌐
   - **ไม่มี**ปุ่ม "Create Bucket"
   - **ไม่มี**ปุ่ม Edit ที่ bucket

4. **สิทธิ์ที่มี:**
   - ดูไฟล์ใน public bucket
   - อัปโหลดไฟล์ไปยัง public bucket
   - ดาวน์โหลดไฟล์จาก public bucket
   - ลบไฟล์ใน public bucket

5. **สิ่งที่ทำไม่ได้:**
   - ไม่เห็น private bucket
   - ไม่สามารถสร้าง/ลบ bucket
   - ไม่สามารถแก้ไข bucket policy

## 🎨 UI Features

### หน้า Dashboard
- **User Info:** แสดงชื่อผู้ใช้และ role badge
- **Bucket Icons:**
  - 🌐 = Public bucket (ทุกคนเข้าถึงได้)
  - 🔒 = Private bucket (เฉพาะ admin)
- **Create Bucket:** ปุ่มสร้าง bucket (admin only)
- **Edit Bucket:** ปุ่มแก้ไข bucket (admin only)
- **Sign Out:** ออกจากระบบ

### หน้า Login/Register
- ฟอร์มสวยงาม gradient background
- แสดง error message เมื่อกรอกผิด
- มี loading state ขณะ login
- แสดงบัญชีทดสอบให้ดู

## 🔧 การจัดการ Bucket Policy

Admin สามารถตั้งค่า policy ของ bucket ได้ 3 แบบ:

### 1. Private (ส่วนตัว)
- เฉพาะ admin เท่านั้นที่เข้าถึงได้
- User ทั่วไปจะไม่เห็น bucket นี้เลย

### 2. Public Read (สาธารณะ - อ่านได้)
- ทุกคน (รวม user) เข้าถึงได้
- User สามารถดู/ดาวน์โหลด/อัปโหลด/ลบได้

### 3. Authenticated Read (ผู้ใช้ที่ login)
- ต้อง login ก่อนถึงเข้าถึงได้
- เหมาะสำหรับข้อมูลที่ต้องการให้เฉพาะผู้ใช้ที่ลงทะเบียนเห็น

## 🆕 สร้างผู้ใช้ใหม่

### ผ่านหน้า Register
1. เปิด `http://localhost:3000/register`
2. กรอก username และ password
3. กด Create account
4. ผู้ใช้ใหม่จะได้ role เป็น USER โดยอัตโนมัติ

### เปลี่ยน User เป็น Admin
ต้องเข้าไปแก้ไขใน database โดยตรง:

```bash
# เปิด Prisma Studio
npx prisma studio

# หรือใช้ SQL command
sqlite3 prisma/dev.db
UPDATE User SET role = 'ADMIN' WHERE username = 'ชื่อผู้ใช้';
.exit
```

## 📊 Database

ข้อมูลผู้ใช้เก็บอยู่ใน SQLite database:
- **Location:** `prisma/dev.db`
- **Table:** User
- **Fields:** id, username, password (hashed), role, createdAt, updatedAt

ดูข้อมูลได้ด้วย Prisma Studio:
```bash
npx prisma studio
```

## 🔒 ความปลอดภัย

✅ **ที่ทำไว้แล้ว:**
- รหัสผ่านถูกเข้ารหัสด้วย bcrypt
- ใช้ JWT token สำหรับ authentication
- Token หมดอายุใน 7 วัน
- ตรวจสอบสิทธิ์ทุก API endpoint

⚠️ **สำหรับ Production:**
- เปลี่ยน `JWT_SECRET` เป็นค่าที่ซับซ้อนและยาวกว่า 32 ตัวอักษร
- ใช้ HTTPS
- เพิ่ม rate limiting
- เพิ่มการตรวจสอบความแข็งแรงของรหัสผ่าน

## 🐛 แก้ไขปัญหา

### ไม่สามารถ login ได้
```bash
# ลอง seed database ใหม่
pnpm db:seed
```

### ไม่มีไฟล์ database
```bash
# รัน migration ใหม่
npx prisma migrate dev
pnpm db:seed
```

### Token หมดอายุ
- กด Sign Out แล้ว Login ใหม่

### เห็น bucket แต่เข้าไม่ได้ (403 Error)
- ตรวจสอบว่า bucket นั้นเป็น public หรือไม่
- ถ้าเป็น user ธรรมดา จะเข้าได้เฉพาะ public bucket
- Login เป็น admin ถ้าต้องการเข้า private bucket

## 📚 เอกสารเพิ่มเติม

- `AUTH_SETUP.md` - คู่มือละเอียดเกี่ยวกับระบบ authentication
- `IMPLEMENTATION_SUMMARY.md` - สรุปการทำงานของระบบทั้งหมด
- `README.md` - คู่มือหลักของโปรเจค

## ✨ สิ่งที่เปลี่ยนไป

### ก่อนมีระบบ Auth
- เปิดแล้วเข้าได้เลย
- ไม่มีการตรวจสอบผู้ใช้
- ทุกคนเห็นทุก bucket
- ทุกคนทำทุกอย่างได้

### หลังมีระบบ Auth ✅
- ต้อง login ก่อนใช้งาน
- แยกสิทธิ์ระหว่าง Admin และ User
- User เห็นเฉพาะ public bucket
- มีการป้องกันข้อมูลที่เป็น private

## 🎯 Next Steps

1. ลองใช้งานทั้ง 2 บัญชี (admin และ user)
2. ทดสอบสร้าง bucket ด้วย admin
3. ตั้ง policy เป็น public แล้วลอง login เป็น user ดู
4. ลองสมัครสมาชิกใหม่ผ่านหน้า register
5. เปลี่ยน JWT_SECRET ก่อน deploy จริง

---

**สนุกกับการใช้งาน!** 🚀

หากมีปัญหาหรือคำถาม สามารถเปิด issue หรือดูเอกสารเพิ่มเติมได้จากไฟล์ที่แนบมา

